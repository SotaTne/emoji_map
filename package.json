{
  "name": "emoji-map-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo 'Please use `mise dev`' && exit 1",
    "build": "echo 'Please use `mise build`' && exit 1",
    "lint": "echo 'Please use `mise lint`' && exit 1",
    "typecheck": "echo 'Please use `mise typecheck`' && exit 1",
    "emulator": "echo 'Please use `mise emulator`' && exit 1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "eslint": "^10.2.1",
    "prettier": "^3.8.3",
    "typescript": "~6.0.2"
  },
  "pnpm": {
    "neverBuiltDependencies": [],
    "onlyBuiltDependencies": [
      "@firebase/util",
      "protobufjs",
      "unrs-resolver"
    ]
  }
}
