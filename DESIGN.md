# Emoji Map デザイン仕様書 (DESIGN)

## 1. デザインコンセプト
- ダークテーマ固定 (#0e0e0e)
- 地図が主役、UIは最小限
- Instagramのような洗練された清潔感と、ゲーム的な楽しさの融合

## 2. カラーパレット (Tailwind v4 連携)
- `bg`: `#0e0e0e`
- `surface`: `#1c1c1c`
- `accent`: `#6b5ce7` (Purple)
- `accentBg`: `#2a2040`
- `text`: `#f0f0f0`
- `textSub`: `#888888`

## 3. 主要コンポーネントのデザイン基準

### 3-1. 投稿ピン (PostPin)
- 吹き出し形状 (14px rounded)
- 絵文字 + 8文字以内のテキスト
- 通常: `#1c1c1c` / ミッション達成時: `#6b5ce7`

### 3-2. 投稿フォーム (PostForm)
- 地図下部中央にフローティング配置
- `EmojiPicker` (左) + `Input` (中央) + `SendButton` (右)
- `rounded-2xl`, `#1c1c1c` 背景

### 3-3. サイドバー
- PC: 常時表示 (210px) / モバイル: ドロワー
- XPバー: 2px高さのパープルゲージ
- ミッション一覧: 絵文字付きリスト、ロック状態は `opacity: 0.4`

## 4. レスポンシブ
- `md: (768px)` をブレークポイントとして活用
- モバイル時はハンバーガーメニューによるサイドバー開閉
