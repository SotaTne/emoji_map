# Emoji Map デザイン仕様書 (DESIGN)

## 1. デザインコンセプト
- ライトモードを基本にする
- 地図が主役、UIは最小限
- Instagramのような洗練された清潔感と、ゲーム的な楽しさの融合
- 入力やピンは小さく、丸みと影で地図から少し浮かせる

## 2. カラーパレット (Tailwind v4 連携)
- `bg`: `#f7f8fb`
- `surface`: `#ffffff`
- `accent`: `#6b5ce7` (Purple)
- `accentBg`: `#f1edff`
- `text`: `#171717`
- `textSub`: `#9a9aa2`
- `textSubtle`: `#5f6169`

## 3. 主要コンポーネントのデザイン基準

### 3-1. 投稿ピン (PostPin)
- 吹き出し形状 (14px rounded)
- 通常表示は絵文字 + 5文字以内のテキスト。6文字以上は `...` で省略
- 通常: 白背景 + 薄いボーダー + 影 / ミッション達成時: `#6b5ce7`
- SVG のティアドロップ形で、一般的な地図ピンに近い形にする
- クリック/タップすると詳細表示になり、日付、時刻、絵文字、一言全文を表示する
- 吹き出し形状は Google Maps の `mapId` がある場合に使う
- `mapId` がない環境ではピンを描画しない

### 3-2. 投稿フォーム (PostForm)
- 地図下部中央にフローティング配置
- `EmojiPicker` (左) + `Input` (中央) + `SendButton` (右)
- Instagram や MUI の入力バーに近い、白背景 + 角丸 + 強めの影
- ラベル `TODAY'S MOOD` を小さく表示し、入力は1行で軽く見せる
- 地点未選択時は placeholder で地図タップを促し、投稿ボタンは disabled
- 地点選択後に送信できる

### 3-3. 選択中ピン (SelectedPin)
- 地図クリック/タップ後、投稿前の地点に表示
- `#6b5ce7` 背景の小さなラベルと下向き三角形
- 通常の投稿ピンより目立つようにリング、ドット、影を付ける
- クリック/タップで地点選択を解除できる
- 投稿成功後は非表示

### 3-4. 投稿ボタン (SendButton)
- 34px x 34px の角丸ボタン
- `#6b5ce7` 背景
- アイコンは `lucide-react` の `ArrowRight`
- disabled は薄いグレー背景

### 3-5. サイドバー
- PC: 常時表示 (210px) / モバイル: ドロワー
- XPバー: 2px高さのパープルゲージ
- ミッション一覧: 絵文字付きリスト、ロック状態は `opacity: 0.4`

## 4. レスポンシブ
- `md: (768px)` をブレークポイントとして活用
- モバイル時はハンバーガーメニューによるサイドバー開閉
